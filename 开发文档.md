# 快递打包监控（双摄版）开发交接文档

> **给新开发者 / 新 AI 的说明**：本文档是唯一权威开发指引，自包含、与账号无关。
> 即使没有历史对话记录，**只读本文档 + 仓库代码**即可独立继续开发。
> 功能差异见《功能差异说明.md》；两者都在仓库根目录，随代码一起提交。

---

## 1. 项目是什么

- 快递打包门店的**双摄像头录像 + 面单识别 + 语音播报 + 画中画取证**桌面软件（WPF）
- 从 [PackingProof/PackingProof-Desktop](https://github.com/PackingProof/PackingProof-Desktop) 二次开发（AGPL-3.0），产品名「快递打包监控（双摄版）v1.2」
- 主功能：扫码触发双路录像（主摄像头拍打包过程 + 扫描摄像头拍面单）、PIP 合成、AI 语音、历史回放、统计
- 另有**独立合并工具** `PackingPipTool/`（.NET 8 WinForms，单文件 exe，零依赖），详见 §12

## 2. 环境准备（开发机硬编码路径）

| 项 | 路径 / 说明 |
|---|---|
| 代码根目录 | `C:\Users\Administrator\WorkBuddy\2026-08-19-15-33-52\PackingProof-Desktop` |
| .NET SDK | `C:\Users\Administrator\.dotnet\dotnet.exe`（.NET 9 SDK，构建目标是 net8.0-windows） |
| 7z 压缩 | `PackingProof-Desktop\package\build-tools\7zip\7z.exe` |
| 依赖源目录 | `PackingProof-Desktop\package\v17-release\publish\`（tts_cache、WinTts.dll、SDK.dll、WinRT.dll 都从这复制；历史 v12-final 等旧版本目录已清理） |
| 发布目录命名 | `package\vXX-release\publish\`（如 v17-release） |
| 测试工程 | `ExpressPackingMonitoring.Tests`（本机缺 net8 运行时跑不了，靠**核对断言逻辑**代替） |
| 日志位置 | `%LOCALAPPDATA%\ExpressPackingMonitoring\log\runtime.log`（运行期问题先查这个，搜 `[PIP]`、`[Recording]`） |

## 3. Git 与仓库约定（重要）

- **origin** = 上游 gitee 镜像 `https://gitee.com/PackingProof/PackingProof-Desktop.git`（fetch 上游更新走它；沙箱 TLS 限制时 GitHub 直连也可能失败，gitee 最稳）
- **推送目标 fork** = `https://github.com/sddvcm/PackingProof-Desktop.git`，分支 `main`
- **Token 机制（红线）**：GitHub 推送/Release 需要 token。**token 由用户提供、每次使用后立即遗忘，绝不写入任何文件、绝不写入 git remote 配置**。推送方式：
  ```bash
  git push "https://<token>@github.com/sddvcm/PackingProof-Desktop.git" main
  ```
- git identity（仅本仓库，非 global）：`WorkBuddy <workbuddy@local>`
- **上游同步**（上游发布新版本时）：
  ```bash
  git fetch origin main          # 拉上游（origin=gitee 镜像）
  git merge origin/main          # 合并；冲突手动解决（两边都保留，见 §8）
  git push "https://<token>@github.com/sddvcm/PackingProof-Desktop.git" main
  ```
- 本地提交规范：`git add -A && git commit -m "feat|fix|docs: 描述"`

## 4. 代码结构总览

| 目录/文件 | 职责 |
|---|---|
| `ExpressPackingMonitoring/Config/AppConfig.cs` | 全部设置项（PipWidth、EnablePipComposite、ActivityLogClearedBeforeUtc、MaximizeVolumeForSpeech 等）；`NormalizeAfterLoad()` 加载归一化 |
| `Config/AppPaths.cs` | 路径（用户数据在 `%LOCALAPPDATA%\ExpressPackingMonitoring`）、`FindFFmpeg()`（优先 `tools\ffmpeg.exe`，回退 PATH） |
| `ViewModels/MainViewModel.cs` | 业务中枢：`CompositePipVideo()`（PIP 合成）、`RebuildMissingPipVideosAsync()`（手动补合）、`ClearActivityLog()`、`RestoreRecentScanRecords()` |
| `ViewModels/MainViewModel.Recording.cs` | 录制生命周期、PIP 调用、MKV→MP4 兜底转换 |
| `Audio/SpeechService.cs` | EdgeTTS / SherpaOnnx 语音；`ApplyPlaybackVolumePolicy()` 音量策略 |
| `Data/VideoDatabase.cs` | SQLite；`VideoRecord`（FilePath=主视频）、`QueryVideos`、`GetRecentCompletedVideos` |
| `UI/MainWindow.xaml(.cs)` | 主窗口（**标题两处改**：XAML Title + `MainWindow.xaml.cs:238` 固定赋值） |
| `UI/SettingsWindow.xaml(.cs)` | 设置/关于/反馈（Feedback_Click 已改为 GitHub Issue） |
| `UI/ScanCameraPreviewWindow.xaml(.cs)` | 扫码悬浮窗（WindowChrome 自绘标题栏 + 最小化/关闭 + 尺寸记忆） |
| `Services/` | 更新服务（LauncherUpdateService 含 GitHub 下载地址）、备份、归档 |
| `Scripts/快递助手订单推送.user.js` | 订单推送油猴脚本（含仓库图标地址） |

## 5. 双摄版 v1.1 二次开发改动清单

| # | 功能 | 关键实现位置 / 设计决策 |
|---|---|---|
| 1 | 双摄像头 | 主+扫描独立配置；扫描支持 USB(AForge)/网络(RTSP/RTMP/HTTP)；旋转 0/90/180/270° |
| 2 | 画中画 PIP | `EnablePipComposite`（默认 true）；`PipWidth` 手动像素宽；位置 `PipPosition`；目录留 3 视频（主.mp4/_scan.mp4/.pip.mp4） |
| 3 | PIP 补合 | 动态标题行「合成画中画」按钮 → `RebuildMissingPipVideosAsync`：DB 遍历 → 正则匹配主/scan 对 → 逐条合成，只生成文件不动 DB |
| 4 | 保持音量开关 | `MaximizeVolumeForSpeech`（true=播报前取消静音+调最大；false=不动音量，默认）；`SpeechService.ApplyPlaybackVolumePolicy()` |
| 5 | 悬浮窗 | WindowChrome 自绘标题栏+1px 边框；「—」「✕」并排右上角；`ScanPreviewWidth/Height` 尺寸记忆 |
| 6 | 动态彻底清除 | `ActivityLogClearedBeforeUtc` 持久化时间戳；`ClearActivityLog` 设置+SaveConfig；`RestoreRecentScanRecords` 过滤 |
| 7 | 品牌化 | 标题「快递打包监控（双摄版）1.1」；csproj `<Version>1.1</Version>`；仓库地址全改 sddvcm（SettingsWindow/LauncherUpdateService/BackupCompatibilityPolicy/Launcher/Installer/Scripts） |
| 8 | 反馈改 Issue | `Feedback_Click`：打包反馈 zip → 打开 `issues/new?title=...`，zip 手动上传；不再用邮件 |
| 9 | 合并时机开关 | `AutoCompositePip`（默认 true=自动合并）；false=手动合并：录制完成跳过自动合成（不弹提示），发货结束后点主界面「合成画中画」（`RebuildMissingPipVideosAsync`）统一补合；设置页「合并时机」下拉框（自动/手动） |
| 10 | 独立合并工具 | `PackingPipTool/`（.NET 8 WinForms，单文件 exe，零 .NET 依赖）；扫描订单号父目录下所有子目录，找缺 PIP 的（同时有 `*_发货.{mp4,mkv}` + `*_scan.{mp4,mkv}` 且无 `*.pip.mp4`）批量合成；照搬主项目 ffmpeg filter_complex；详见 §12 |

## 6. 构建 → 打包 → 发布（标准命令）

```bash
# 1. 构建（必须 -p:NuGetAudit=false，否则 NuGet 审计写缓存被文件锁，报 MarkupCompile Access denied）
"C:\Users\Administrator\.dotnet\dotnet.exe" build ExpressPackingMonitoring.sln -p:NuGetAudit=false -nodeReuse:false -m:1

# 2. 发布自包含包（vXX = 下一个版本号目录）
"C:\Users\Administrator\.dotnet\dotnet.exe" publish ExpressPackingMonitoring/ExpressPackingMonitoring.csproj \
  -c Release -r win-x64 --self-contained true -o package/vXX-release/publish -p:NuGetAudit=false

# 3. 补齐依赖（publish 不带这 4 项，从 v17-release（最新版本目录）复制；tts_cache 是目录）
cp -r package/v17-release/publish/tts_cache package/vXX-release/publish/tts_cache   # 目标已存在会嵌套！先删或 if [ ! -d ]
cp package/v17-release/publish/ExpressPackingMonitoring.WinTts.dll \
   package/v17-release/publish/Microsoft.Windows.SDK.NET.dll \
   package/v17-release/publish/WinRT.Runtime.dll package/vXX-release/publish/

# 4. 压缩（zip 根目录必须是 publish\，与历史版本一致）
cd package/vXX-release && 7z a -tzip -mx=5 目标目录\ExpressPackingMonitoring-dual-camera-vXX.zip publish

# 5. 提交 + 推送 fork（token 问用户要，一次性）
git add -A && git commit -m "..." && git push "https://<token>@github.com/sddvcm/PackingProof-Desktop.git" main
```

发布 Release（gh 不可用，用 REST API）：
```bash
# 创建（tag 建议 vX.Y 与版本号一致）
curl -X POST "https://api.github.com/repos/sddvcm/PackingProof-Desktop/releases" \
  -H "Authorization: Bearer <TOKEN>" -H "Accept: application/vnd.github+json" \
  -d '{"tag_name":"v1.1","name":"快递打包监控（双摄版）v1.1","body":"说明","draft":false,"prerelease":false}'
# 上传资产（<RELEASE_ID> 从创建响应拿）
curl -X POST "https://uploads.github.com/repos/sddvcm/PackingProof-Desktop/releases/<RELEASE_ID>/assets?name=xxx.zip" \
  -H "Authorization: Bearer <TOKEN>" -H "Content-Type: application/zip" --data-binary @xxx.zip
```

## 7. 常见坑排查大全

| 现象 | 原因 | 解决 |
|---|---|---|
| PIP 不生成，目录只有 _scan.mp4 + 主.mp4 | ① EnablePipComposite=false ② scan.mkv 损坏（录制端问题）③ scale 表达式错 ④ AutoCompositePip=false（手动模式，需点「合成画中画」补合） | 看日志 `[PIP]` 行；损坏 scan 无法补合，需删/重录；手动模式属预期行为 |
| PIP 报 `Option 'scale' not found` | scaleExpr 带了 `scale=` 前缀，与 filter_complex 模板重复拼接成 `scale=scale=...` | scaleExpr 必须是**纯 args**（如 `640:-1`） |
| 构建报 `MarkupCompilePass1 ... Access denied` | NuGet 漏洞审计写缓存被锁 | 加 `-p:NuGetAudit=false`；仍失败删 obj/bin + `-nodeReuse:false -m:1` |
| zip 覆盖报"拒绝访问" | 旧 zip 文件锁 | 换新文件名交付（vXX-fixN.zip） |
| tts_cache 文件数 673（应 611） | `cp -r` 目标已存在嵌套成 tts_cache/tts_cache | 用 `if [ ! -d tts_cache ]; then cp -r ...; fi` 或 PowerShell `Remove-Item -LiteralPath ... -Recurse -Force` |
| 连续快速发货时系统卡住 | 每单录制结束立即 ffmpeg 合成 PIP，任务堆积占满 CPU/磁盘 | 设置页把「合并时机」改为「手动合并」，发货结束后点主界面「合成画中画」统一补合 |
| 日志乱/看不到运行记录 | 沙箱里 `%LOCALAPPDATA%` 有自动化测试残留（epm-*-loopback-*） | 用户机器日志才是真的；让用户复制 runtime.log |
| 主窗口标题没变 | `MainWindow.xaml.cs:238` 运行时覆盖 XAML | 两处都要改（XAML Title + cs:238 固定字符串） |

## 8. 上游同步冲突处理原则

- 冲突集中在两边都改过的文件：`MainViewModel.cs`、`SettingsWindow.*`、`AppConfig.cs`、`csproj`、`MainWindow.xaml`
- **原则：两边都保留** —— 上游新功能 + 本 fork 二次开发（PIP、保持音量、仓库地址、标题）
- 解决：编辑冲突文件 → `git add` → `git commit`；推荐 merge（不 force push，保护已发布 Release）

## 9. 版本管理约定

- 版本号唯一来源：`ExpressPackingMonitoring/ExpressPackingMonitoring.csproj` 的 `<Version>`
- 窗口标题里版本号是**写死的**（"（双摄版）1.1"），发版时**两处都要改**：csproj `<Version>` + `MainWindow.xaml`/`MainWindow.xaml.cs:238` 标题
- Release tag 建议 = `v` + 版本号（如 v1.1）；zip 文件名用递增的 vXX（如 v18）
- 发版完整链路：改版本 → 构建 → publish → 补依赖 → 压缩 → commit → push fork → 创建 Release + 传资产

## 10. 新开发者上手清单（Checklist）

- [ ] 读本文档 §1-§3（项目、环境、Git）
- [ ] 构建一次通过（§6 命令）
- [ ] 读 `MainViewModel.cs` 的 `CompositePipVideo` 和 `RebuildMissingPipVideosAsync`（本 fork 核心改动）
- [ ] 读 `AppConfig.cs` 新增字段及 `SettingsWindow` 对应 UI
- [ ] 做一个小改动（如改个提示文案）→ 构建 → 打包 → 验证全链路
- [ ] 遇到问题先查日志（§2 日志位置）+ §7 坑表

## 11. 红线（务必遵守）

- **token 不落盘**：每次向用户索取，用完即忘，不写 remote/文件
- **不删除 `.workbuddy` 目录**（项目记忆/数据）
- 不改 `origin`（保持 gitee 上游），推送一律一次性 URL
- 反馈包含订单数据库/隐私，**提醒用户不要公开上传到 Issue**
- 打包 zip 结构保持 `publish\` 根目录，与历史版本（V12 起）一致

## 12. 独立合并工具 PackingPipTool

适用于**主程序未运行 / 历史录像需补合**的场景。零依赖（self-contained win-x64），把生成的 `PackingPipTool.exe` 复制到「订单号父目录」下双击即可。

### 12.1 功能
- 扫描根目录下所有订单号子目录，**自动识别**满足以下全部条件的待合并项：
  - 存在 `*_scan.{mp4,mkv}`（**以 scan 文件为锚点**，不依赖主视频 mode 名称）
  - 找到同 prefix 的非 scan 视频作主视频（`{prefix}_发货.mp4`、`{prefix}_退货.mp4` 等任意 mode 均可）
  - **不存在**对应的 `*.pip.mp4`（即未合并过）
- UI：候选列表（订单目录 / 主视频 / 扫描视频 / PIP 输出 / 状态）、进度条、状态标签（"正在合并 X/Y，已完成 A（成功 P，失败 Q）"）、实时日志面板、停止按钮
- **默认值：PIP 位置=左上（TopLeft）、PIP 宽度=640px**
- ffmpeg filter_complex 基于主项目 `CompositePipVideo`，但**加了 `setpts=PTS-STARTPTS`**：让主/扫描两路都从 PTS=0 强制对齐，解决历史文件录制起点不一致导致的画面不同步（主项目 RebuildMissingPipVideosAsync 无 setpts，对刚录完的文件调用差异不显）；5 分钟超时；失败清理空文件 + 标红

### 12.2 使用步骤
1. **发布**（开发机执行一次）：
   ```bash
   cd PackingProof-Desktop/PackingPipTool
   "C:\Users\Administrator\.dotnet\dotnet.exe" publish PackingPipTool.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:EnableCompressionInSingleFile=true -p:NuGetAudit=false -o publish
   ```
   产物：`publish/PackingPipTool.exe`（**约 66 MiB**，启用单文件压缩）
2. **分发**：把 `PackingPipTool.exe` 复制到用户的「订单号父目录」（与各订单号子目录同级）
3. **运行**：双击 `PackingPipTool.exe`
   - 启动时根目录自动填 exe 所在目录
   - ffmpeg 自动探测：同目录 `tools\ffmpeg.exe` → 同目录 `ffmpeg.exe` → PATH；探测不到在 UI 顶部手选
4. 点「**扫描**」 → 列表显示待合并项 → 调好位置/宽度 → 点「**开始合并**」

### 12.3 文件结构
```
PackingProof-Desktop/PackingPipTool/
├── PackingPipTool.csproj   # .NET 8 WinForms，<PublishSingleFile>true 自包含 + 单文件压缩
├── Program.cs              # 入口
├── MainForm.cs             # UI + 扫描 + ffmpeg 调用（含 FlatButton 模拟按钮 + setpts 同步）
├── app.manifest            # Windows 兼容性
├── .gitignore              # 忽略 bin/obj/publish
└── publish*/               # git 忽略；本地发布产物（publish/publish_new/publish_v3/publish_final）
    └── PackingPipTool.exe  # 单文件 exe，给用户复制
```

### 12.4 命名匹配规则（可改）
- 扫描：`{prefix}_scan.{mp4,mkv}`（以 scan 文件驱动，提取 prefix = 去掉最后一段 `_scan`）
- 主视频：同目录下以 `{prefix}_` 开头的非 scan 视频（`{prefix}_发货.mp4`、`{prefix}_退货.mp4` 等）
- PIP：`<原主视频基名>.pip.mp4`（即 `Path.GetFileNameWithoutExtension(main) + ".pip.mp4"`）
- 扫描逻辑在 `MainForm.cs` 的 `ScanRoot`；若命名规则变化，改那里的枚举/前缀规则即可

### 12.5 踩坑记录（self-contained WinForms 单文件）
| 现象 | 根因 | 解决 |
|---|---|---|
| Button/Label 文字不显示或乱码 | 早期误判为 `EnableCompressionInSingleFile` 破坏 GDI+ 字体句柄（关闭压缩无效，Label 修好 Button 仍空白） | 真根因是**布局**：Button 用 GDI+ `Graphics.DrawString` 渲染文字，self-contained 下单文件解压时机偶发异常；最终用 `FlatButton : Label`（GDI TextRenderer 画文字）+ **手动 Bounds 定位**（放弃 TableLayoutPanel+Dock=Fill，后者与 Label 默认 PreferredSize 冲突会把文字画到控件外）解决 |
| `Label` 文字画到控件矩形外（按钮下方一行小字） | Label 默认 `AutoSize=true` 按文字 PreferredSize 自调大小，与 `Dock=Fill` 冲突，ClientRectangle 与文字位置错位 | ① `AutoSize=false` ② 最终方案：不用 Dock=Fill，改 `Bounds = new Rectangle(x, y, w, h)` 手动定位 |
| publish 报 `GenerateBundle ... file in use` | 旧 exe 被正在运行的进程占用 | 换新输出目录（`publish_new` / `publish_v3` / `publish_final`）避开文件锁 |
