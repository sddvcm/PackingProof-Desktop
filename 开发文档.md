# 快递打包监控（双摄版）开发文档

> 本项目从 [PackingProof/PackingProof-Desktop](https://github.com/PackingProof/PackingProof-Desktop) 二次开发（AGPL-3.0）。
> 本文档面向后续开发者，覆盖：代码结构、二次开发改动、构建打包、发布、常见坑。

## 1. 项目概览

| 项 | 值 |
|---|---|
| 产品名 | 快递打包监控（双摄版）v1.1 |
| 技术栈 | C# / WPF / .NET 8 (win-x64)，NuGet: OpenCV、AForge.Video、FFmpeg、NAudio、ZXing、EdgeTTS、SherpaOnnx |
| 主工程 | `ExpressPackingMonitoring/ExpressPackingMonitoring.csproj` |
| 测试工程 | `ExpressPackingMonitoring.Tests`（net8 运行时缺失时跑不了，靠核对断言） |
| 构建工具 | `C:\Users\Administrator\.dotnet\dotnet.exe`（.NET 9 SDK） |

## 2. Git 与仓库约定

- `origin` = 上游 gitee 镜像 `https://gitee.com/PackingProof/PackingProof-Desktop.git`（沙箱 TLS 限制，clone/fetch 走 gitee；GitHub 直连一般也可用）
- **推送目标 fork** = `https://github.com/sddvcm/PackingProof-Desktop.git` 分支 `main`
- 推送方式：一次性 token 拼 URL `git push "https://<token>@github.com/sddvcm/PackingProof-Desktop.git" main`，**不写入 git remote**
- git identity（仅本仓库）：name=`WorkBuddy`、email=`workbuddy@local`
- 上游同步：`git fetch upstream && git merge upstream/main`（推荐 merge，不 force push）

## 3. 核心模块（源代码位置）

| 模块 | 位置 | 说明 |
|---|---|---|
| 配置 | `Config/AppConfig.cs` | 所有设置项；`NormalizeAfterLoad()` 加载时归一化；设置窗口用**克隆副本**编辑后 `ApplySettingsAsync` 应用 |
| 配置路径 | `Config/AppPaths.cs` | 用户数据在 `%LOCALAPPDATA%\ExpressPackingMonitoring`（config.json、videos.db、log/runtime.log） |
| 主视图 | `ViewModels/MainViewModel.cs` | 业务中枢；`CompositePipVideo`（PIP 合成）、`RebuildMissingPipVideosAsync`（补合）、`RestoreRecentScanRecords`（动态恢复） |
| 录像流程 | `ViewModels/MainViewModel.Recording.cs` | 录制开始/结束、PIP 合成调用、MKV→MP4 兜底转换 |
| 语音 | `Audio/SpeechService.cs` | EdgeTTS/SherpaOnnx 播报；`ApplyPlaybackVolumePolicy()` 音量策略（保持系统音量开关） |
| 数据库 | `Data/VideoDatabase.cs` | SQLite；`VideoRecord`（FilePath 指向主视频）、`QueryVideos`、`GetRecentCompletedVideos` |
| 设置窗口 | `UI/SettingsWindow.xaml(.cs)` | 关于页、反馈打包、反馈渠道（GitHub Issue） |
| 悬浮窗 | `UI/ScanCameraPreviewWindow.xaml(.cs)` | WindowChrome 自绘标题栏、最小化/关闭、尺寸记忆 |

## 4. 双摄版 v1.1 二次开发改动清单

1. **双摄像头**：主摄像头 + 扫描摄像头独立配置（分辨率/帧率/编码/旋转 0/90/180/270°），扫描支持 USB(AForge)/网络(RTSP/RTMP/HTTP)
2. **画中画（PIP）**：
   - `AppConfig.EnablePipComposite`（默认 true）+ `PipWidth`（手动输入像素宽）+ `PipPosition`
   - `CompositePipVideo()` 合成表达式：`scale={pipWidth}:-1`（**纯 args，不带 `scale=` 前缀**，filter_complex 模板自带）
   - 目录保留 3 个视频：`主.mp4` / `扫描_scan.mp4` / `PIP.pip.mp4`
   - `RebuildMissingPipVideosAsync()`：右侧动态标题行「合成画中画」按钮，批量补合历史录像
3. **语音保持系统音量**：`AppConfig.MaximizeVolumeForSpeech`（true=播报前取消静音+调最大；false=不碰音量，默认），`SpeechService.ApplyPlaybackVolumePolicy()`
4. **扫码悬浮窗**：WindowChrome 自绘标题栏+边框；「—」最小化（左）「✕」关闭（右）并排；`ScanPreviewWidth/Height` 尺寸记忆
5. **本机录制动态**：`ClearActivityLog()` 彻底清除（`AppConfig.ActivityLogClearedBeforeUtc` 持久化时间戳，重启不恢复）；`RestoreRecentScanRecords` 过滤该时间戳前记录
6. **品牌化**：主窗口标题「快递打包监控（双摄版）1.1」（`MainWindow.xaml` Title + `MainWindow.xaml.cs:238` 固定赋值，**两处都要改**）；csproj `<Version>1.1</Version>`；所有仓库地址指向 `https://github.com/sddvcm/PackingProof-Desktop`（SettingsWindow 打开仓库/LICENSE、LauncherUpdateService、BackupCompatibilityPolicy、Launcher/Program.cs、Installer .iss、Scripts/*.js）；关于页注明二次开发来源
7. **反馈渠道**：`SettingsWindow.xaml.cs` Feedback_Click 从邮件（Outlook/mailto）改为 GitHub Issue（打包反馈包 zip → 打开 `issues/new?title=...`，zip 手动上传为附件）

## 5. 构建与打包（标准流程）

```bash
# 构建（必须加 NuGetAudit=false，否则 NuGet 漏洞审计写缓存会被文件锁拦截）
dotnet build ExpressPackingMonitoring.sln -p:NuGetAudit=false -nodeReuse:false -m:1

# 发布自包含包
dotnet publish ExpressPackingMonitoring/ExpressPackingMonitoring.csproj \
  -c Release -r win-x64 --self-contained true \
  -o package/vXX-release/publish -p:NuGetAudit=false
```

发布后补齐依赖（publish 不带这 4 项，从 `package/v12-final/publish/` 复制）：
- `tts_cache`（目录；**若目标已存在不要再 cp -r，会嵌套成 tts_cache/tts_cache**）
- `ExpressPackingMonitoring.WinTts.dll`、`Microsoft.Windows.SDK.NET.dll`、`WinRT.Runtime.dll`

压缩（zip 根目录必须是 `publish\`，与历史版本一致）：
```bash
cd package/vXX-release && 7z a -tzip -mx=5 目标.zip publish
```

## 6. 发布到 GitHub Releases

gh CLI 不可用，用 REST API（curl）：
```bash
# 创建 release（tag v1.1）
curl -X POST "https://api.github.com/repos/sddvcm/PackingProof-Desktop/releases" \
  -H "Authorization: Bearer <TOKEN>" -H "Accept: application/vnd.github+json" \
  -d '{"tag_name":"v1.1","name":"...","body":"...","draft":false,"prerelease":false}'

# 上传资产（release id 从创建响应取）
curl -X POST "https://uploads.github.com/repos/sddvcm/PackingProof-Desktop/releases/<ID>/assets?name=xxx.zip" \
  -H "Authorization: Bearer <TOKEN>" -H "Content-Type: application/zip" \
  --data-binary @xxx.zip
```

## 7. 常见坑与排查

- **PIP 不生成**：先查 `%LOCALAPPDATA%\ExpressPackingMonitoring\log\runtime.log` 的 `[PIP]` 行（含完整 ffmpeg 命令 + stderr）；常见原因：scan.mkv 损坏（录制端问题，补合无法修复）、scale 表达式错误
- **PIP `Option 'scale' not found`**：scaleExpr 带 `scale=` 前缀重复（应纯 args `640:-1`）
- **构建报 `MarkupCompilePass1 ... Access denied`**：NuGet 漏洞审计文件锁；加 `-p:NuGetAudit=false`；仍失败删 `obj bin`（提权）+ `-nodeReuse:false -m:1`
- **zip 覆盖报"拒绝访问"**：旧 zip 被锁；改用新文件名交付（如 v17-fix3.zip）
- **日志文件**：`%LOCALAPPDATA%\ExpressPackingMonitoring\log\runtime.log`（注意：沙箱里该目录可能有自动化测试残留日志 `epm-*-loopback-*`，非用户真实环境）

## 8. 后续开发标准流程

改代码 → 构建验证（0 错误）→ publish → 补齐依赖 → 7z 压缩 → （可选）commit + 一次性 token push 到 fork → （可选）创建 Release 上传资产。全程保持 zip 结构与 V12 一致（`publish\` 根目录、611 文件量级）。
