# 快递打包监控（双摄版）v1.1 功能差异说明

本项目从 [PackingProof/PackingProof-Desktop](https://github.com/PackingProof/PackingProof-Desktop) 二次开发而来（AGPL-3.0）。
以下列出相对源软件**新增/改进**的功能清单。

## 1. 双摄像头录制

- 独立的**主摄像头 + 扫描摄像头**双路配置：分辨率、帧率、编码、旋转各自独立设置
- 扫描摄像头支持 USB（AForge）与网络（RTSP/RTMP/HTTP）两种来源
- 扫描画面支持 **0 / 90 / 180 / 270° 旋转**，适配面单横竖摆放
- 扫描录像单独成文件（`xxx_scan.mkv`），与主视频一同保留

## 2. 画中画（PIP）合成

- **默认开启**：每次录制结束后自动把扫描画面叠加到主视频角落，生成独立的 `xxx.pip.mp4`
- 单目录同时保留 3 个视频：`主.mp4`、`扫描_scan.mp4`、`PIP.pip.mp4`，方便取证与回放
- **画中画尺寸改为手动输入宽度（像素）**，高度按扫描视频宽高比自动计算；兼容旧比例档位
- 画中画位置可调：左上 / 右上 / 左下 / 右下
- **「合成画中画」一键补合**：对历史录像中缺少 PIP 的视频批量补生成（扫描数据库记录，只生成文件、不动原视频与数据库）
- 合成失败原因直接弹出（ffmpeg stderr 摘要），不再只能查日志
- 修复了源版本 PIP 表达式拼接错误导致的合成失效问题

## 3. AI 语音播报

- 新增 **「保持系统音量」开关**：
  - 打开（默认）：语音播报完全不修改系统音量 / 静音状态
  - 关闭：恢复旧行为，播报前自动取消静音并把音量调到最大
- 开关同时作用于日常语音播报与设置页「试听」

## 4. 扫码摄像头悬浮窗

- **最小化按钮**（与关闭按钮并排、风格一致）：最小化到任务栏可点击恢复
- **尺寸记忆**：拖拽调整窗口大小后，下次启动软件自动恢复上次尺寸
- 自绘标题栏 + 窗口边框，拖动 / 边缘缩放正常

## 5. 本机录制动态列表

- 标题栏新增 **「清除动态记录」** 按钮：彻底清除（记录清除时间点，重启软件后不再恢复已清除的记录；清除之后新录制的正常显示）
- 标题栏新增 **「合成画中画」** 按钮：批量补合历史录像的画中画

## 6. 品牌与分发

- 主窗口标题：**「快递打包监控（双摄版）1.1」**，版本号统一为 1.1
- 检查更新 / 打开仓库 / 许可证链接 / 启动器更新源均指向本 fork：<https://github.com/sddvcm/PackingProof-Desktop>
- **反馈流程改为 GitHub Issue**：一键打包运行日志 + 配置 + 数据库为压缩包，点击「打开 GitHub Issue」直接在网页新建反馈（标题预填，压缩包手动作为附件上传），不再依赖邮件客户端
- 关于页注明二次开发来源

## 7. 稳定性与兼容性

- 扫描录像加固：主/扫描 MKV 统一转 MP4 并带降级容错，扫描视频不再遗留无法播放的孤儿文件
- PIP 合成表达式采用最基础写法（`scale=w:-1`），兼容不同 FFmpeg 版本
- 完整附带 FFmpeg、WinTts 语音桥接、TTS 缓存等运行时依赖，开箱即用

---
*构建产物：`ExpressPackingMonitoring-dual-camera-v17.zip`（解压后运行 `publish\ExpressPackingMonitoring.exe`）*
